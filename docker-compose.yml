services:
  app:
    build: .
    container_name: fenomenos_corruptivos_app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
