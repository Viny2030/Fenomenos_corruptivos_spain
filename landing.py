"""
landing.py
==========
DEPRECADO — no lo importa nada. main.py sirve /  con templates/landing_es.html
(antes tenía su propia copia inline de este mismo HTML). Este archivo quedó
huérfano de una iteración anterior; candidato a borrarse (`git rm landing.py`).

Se conserva vacío de HTML (que ya vive en templates/) para no confundir con
una versión desactualizada del contenido real.
"""
